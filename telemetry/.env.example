# Copy to telemetry/.env and edit for local defaults.
FPG_TELEMETRY_PORT=10000
FPG_TELEMETRY_DB=./data/events.db

# Empty means no auth. Set a real secret only in telemetry/.env, not in git.
FPG_TELEMETRY_TOKEN=
