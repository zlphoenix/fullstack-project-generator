# 贡献指南

欢迎贡献新 Skill、平台指南、模板、脚本与文档改进。本指南确保贡献与项目的方法论一致、可维护、可度量。

先读 [AGENTS.md](AGENTS.md)（单一事实源）与 [docs/10-AI辅助开发最佳实践研究报告.md](docs/10-AI辅助开发最佳实践研究报告.md)（设计依据）。

## 一、开发约定

遵循 `AGENTS.md` 的四条行为准则：先思考再编码、简洁优先、外科手术式改动、目标驱动执行。
语言：文档与 Skill **中文为主**，专有名词保留英文；代码注释清晰可读。
技术栈：脚本仅用 **Python（现有 MCP）/ TypeScript(Bun) / shell**，不引入其他开发语言。

## 二、编写或修改 Skill 的规范

依据 Anthropic / OpenAI 的 Skill 最佳实践：

- **frontmatter**：必须含 `name`（小写+连字符，不含保留字）与 `description`（第三人称，写清"做什么 + 何时用 + 触发词"）。
- **渐进式披露**：`SKILL.md` 正文 **< 500 行**；细节拆到 `references/`，引用**只下钻一层**；>100 行的参考文件带目录。
- **结构**：以"前置检查（读 `project-state` MCP）"开始，以"`write_project_state` 持久化"结束。
- **自由度匹配脆弱度**：多解任务给文字指令；脆弱/需一致的操作给"照此命令执行，别改"的脚本。
- **脚本**：解决问题而非甩锅给模型；显式错误处理；无 voodoo 常量（魔数要有注释）；路径用正斜杠；MCP 工具用全限定名。
- **遥测**：新增 Skill 时在**目标项目模板 `project-template/AGENTS.md` §5** 的埋点表登记其事件点，并在 SKILL.md 关键时机调用 `$FPG_HOME/telemetry/emit.sh`（best-effort）。
- **避免时效性信息**（用"old patterns"折叠承载历史）；术语全程一致。

## 三、评估先行（Evaluation-Driven）

**先写评估，再写文档/脚本**：
1. 用代表性任务跑出"无此 Skill 时的失败基线"。
2. 写 ≥ 3 个评估场景覆盖这些缺口。
3. 写最小内容让评估通过，再迭代。
4. 用 Claude A 写 Skill、另一实例实测、观察后回迭代。

## 四、提交前校验（必跑）

```bash
(cd telemetry && bun test)                        # 遥测单元测试通过
```

改了 SKILL.md，自查：正文 < 500 行 / frontmatter 完整 / 引用一层 / 路径有效 / 已登记遥测埋点。
改了脚本，附带或更新对应测试。

## 五、提交规范

- 分支：从默认分支切出特性分支再改。
- 提交信息：用 `type(scope): 简述`（如 `feat(skill): 新增 backend-bun 指南`），正文说清动机。
- PR：描述改了什么、为什么、如何验证；勾选上述校验项。

## 六、不要做

- 不在未沟通时大改方法论内核（契约驱动、单 Story×单平台、Sprint 迭代模式 [ADR-009](docs/00-决策记录.md)）。
- **不再引入 Python**（[ADR-013](docs/00-决策记录.md)）；跨会话进展用项目产物派生 + 遥测事件，不要恢复 MCP/状态文件。
- 不引入除 Java/TS 外的新开发语言；SKILL 客户端埋点仅用 shell+curl。
- 不删除/弱化测试来"让它通过"。
- **不按角色/工作类型拆分多 Agent**（[ADR-008](docs/00-决策记录.md)）：按可隔离的上下文拆分，先单 Agent 后多 Agent，验收与生成分离。
- **harness 不要只增不减**：每个编排/工件组件都要标注"替模型补了什么能力"，随模型升级定期复审、剥离不再承重的组件。
- 写"自动化行为/规则"时优先做成**可见+可强制的环境/门禁**（hooks/CI/linter），而非只写进提示或文档（环境优于提示）。
