# 归档

此目录保留已废弃但有追溯价值的历史文件。

- `SKILL.md.deprecated` —— 最初的**单体** Skill（一个 SKILL.md 覆盖全流程）。已按"单一职责"重构为 `skills/` 下 7 个独立 Skill。其中引用的 `assets/`、`scripts/generate_api_contract.py` 等路径已不再适用，仅供查阅设计演进。当前结构见 [AGENTS.md](../../AGENTS.md)。
