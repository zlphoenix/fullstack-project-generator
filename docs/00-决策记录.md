# 决策记录（ADR）

> 本项目自身按"AI Agent 项目规范"运作：**所有关键决策都落地到本文件**，可追溯、可评审、可版本化。
> 格式：编号 / 日期 / 状态 / 背景 / 决策 / 理由 / 影响。新决策追加在末尾；推翻旧决策时新增一条并把旧条状态改为「已废止（被 ADR-XXX 取代）」。

| 编号 | 决策摘要 | 状态 |
|---|---|---|
| ADR-001 | 同时支持 Claude Code 与 Codex | 生效 |
| ADR-002 | 后端覆盖 Java/Spring 与 TypeScript+Bun；含多项目/多环境/可观测等 | 生效 |
| ADR-003 | 度量采用集中式遥测（TS/Bun+shell），不复用 Python MCP、不用本地日志 | 生效 |
| ADR-004 | 交付文档中文为主 | 生效 |
| ADR-005 | 按 M1→M2→M3 推进，M1 先行再回看 | 生效 |
| ADR-006 | 零依赖 install.sh 分发；Skills-Manager 类工具仅调研 | 生效 |
| ADR-007 | 陈旧文件归档到 `docs/90-归档/` | 生效 |
| ADR-008 | 多 Agent 按可隔离上下文拆分，**不按角色/工作类型** | 生效 |
| ADR-009 | 保留迭代（Sprint）开发模式作为最小交付周期，迭代回顾用观测指标持续改进 | 生效 |
| ADR-010 | 本项目自身遵循 AI Agent 项目规范：决策落文档 + 每主要目录加 AGENTS.md | 生效 |
| ADR-011 | docs 文件按功能分类 + 优先级编号、中文文件名 | 生效 |
| ADR-012 | 度量数据用于改进与验证规范，不作个人绩效考核 | 生效 |
| ADR-013 | 移除 `.project-state.json` 与 `project_state.py`：跨会话进展改由项目产物派生 + 遥测事件统一收集 | 生效 |
| ADR-014 | 工具脚本（init/contract）的语言移植与功能改进推迟 M2；M1 仅移除 project_state.py 与 mcp 依赖 | 生效 |
| ADR-015 | 解散 `skills/shared`：通用规则进目标项目 AGENTS.md；平台指南作项目级公共参考；SKILL 自包含；软链默认项目级、不覆盖 | 生效 |
| ADR-016 | M1 重定义：SKILL 规范完备 + 支持 Claude/Codex + 可分发可更新；度量统一经遥测（客户端仅 curl，尽量不依赖 TS） | 生效 |
| ADR-017 | 工具侧 hook 自动埋点（M2 提前）：用 Codex/Claude hooks 在会话/回合事件自动上报，不依赖模型自觉；Codex 优先 | 生效 |
| ADR-018 | 迭代治理层级统一为 Epic(E) → Sprint(S) → Task(T)；每级 `plan.md` 是唯一计划、清单与指标真源 | 生效 |
| ADR-019 | 度量计时主轴 = Skill 调用 / E·S·T 切片（取消传统阶段 cycle-time，phase 降为软标签，审核/验收=verification 事件）；计划侧经 `plan_sync` 并入遥测与实测 join；身份 id/name 分离 | 生效 |

> ⚠️ ADR-003 部分修订：度量仍是集中式遥测，但**客户端埋点仅用 shell+curl，尽量不依赖 TS**（收集器/报表后端仍为 Bun/TS）。见 ADR-016。

---

## ADR-001 工具范围：Claude Code + Codex 双支持
- **日期** 2026-05-30 ・ **状态** 生效
- **背景** 团队会同时使用多种 AI 编码工具；只面向单一工具会限制落地。
- **决策** 这套 Skill 必须在 **Claude Code 与 Codex** 上都可用；可引入 Skills-Manager 等开源分发工具支撑。
- **理由** 覆盖团队实际工具组合；`AGENTS.md`/`SKILL.md`/stdio MCP 均为跨工具标准。
- **影响** 以 `AGENTS.md` 为单一事实源；`install.sh` 同时配置两工具；细节差异需各自实测。

## ADR-002 技术栈范围
- **日期** 2026-05-30 ・ **状态** 生效
- **背景** 原仅 Java/Spring 后端，且缺多项目/多环境/可观测等长期维护能力。
- **决策** 后端覆盖 **Java/Spring 与 TypeScript+Bun**；并满足多项目管理、多环境隔离（dev/test/staging/prod）、本地开发友好（依赖一致性、独立运行、网络请求可观测）、运行时 HA/可观测/可扩展、可读性、CI+自动发布。
- **理由** 匹配团队真实技术栈与长期维护诉求。
- **影响** M3 落地（新 guide + 模板 + 环境规范）。

## ADR-003 度量：集中式遥测
- **日期** 2026-05-30 ・ **状态** 生效
- **背景** 需要在分发给所有成员后"使用即产生数据、统一收集"用于运营报表；本地日志无法集中。
- **决策** 独立的 `telemetry/` 集中式管线（emit.sh → Bun collector → Bun report），**不复用 Python MCP、不用本地日志**；语言仅 TS(Bun)+shell。
- **理由** 集中可汇聚、跨工具、零额外语言；与状态 MCP 解耦（状态记"是什么"，遥测记"发生了什么"）。
- **影响** 见 `30-度量指南.md`、`telemetry/`。

## ADR-004 文档语言
- 2026-05-30 ・ 生效 ・ **决策** 交付文档（研究/计划/README/度量等）**中文为主**，专有名词保留英文。**理由** 与团队沟通语言一致。

## ADR-005 执行节奏
- 2026-05-30 ・ 生效 ・ **决策** 分 M1（基座）/M2（强制与工件）/M3（栈扩展）三里程碑，**先做 M1 再回看**。**理由** 控制单次改动规模、尽早验证、降低审查负担。**影响** 见 `01-里程碑与验收标准.md`。

## ADR-006 分发工具
- 2026-05-30 ・ 生效 ・ **决策** 先用**零依赖 `install.sh`**；Skills-Manager 类外部工具仅在分发指南中作可选项调研，不引入依赖。**理由** 降低运维面，规模化后再评估。

## ADR-007 陈旧文件
- 2026-05-30 ・ 生效 ・ **决策** `SKILL.md.deprecated` 等陈旧文件移到 `docs/90-归档/` 保留追溯，不直接删除。**理由** 保留设计演进可追溯性。

## ADR-008 多 Agent 拆分维度（原则修正）
- **日期** 2026-05-30 ・ **状态** 生效
- **背景** 补充材料（[多 Agent 何时/如何用](https://claude.com/blog/building-multi-agent-systems-when-and-how-to-use-them)、[Anthropic harness-design](https://www.anthropic.com/engineering/harness-design-long-running-apps)、[OpenAI harness-engineering](https://openai.com/index/harness-engineering/)）指出：按角色/工作类型拆分会制造协调开销、交接丢上下文。
- **决策** 多 Agent **按可隔离的上下文拆分，绝不按角色/工作类型拆分**；先单 Agent，仅在"上下文污染/可并行/需专精"且有证据时才上多 Agent；生成者≠评估者；harness 随模型升级做减法。
- **理由** 降低协调开销与 token 成本（多 Agent 3–10x），提升验收可靠性。
- **影响** 研究报告 §5/§7 已更新；计划 WS0/WS3；待修订 `methodology.md §7.1`。注：遥测 `actor_role` 刻画**人类成员角色**，与此不冲突。

## ADR-009 保留迭代（Sprint）开发模式
- **日期** 2026-05-30 ・ **状态** 生效
- **背景** 基于对团队开发水平的评估，以及当前 AI 协作中存在的问题，暂不激进切换到纯交付驱动模式。
- **决策** **保留迭代开发模式，以 Sprint 作为最小交付周期**；每个迭代结束的回顾会**根据收集的迭代观测指标**（返工率、交付时长、AC 通过率、阶段周期时间等）评估并持续改进。
- **理由** 迭代节奏与团队现状匹配，且天然契合遥测度量与持续改进闭环。
- **影响** `methodology.md` 的 Sprint 流程保留；回顾会消费 `telemetry/` 报表；后续可随团队成熟度再评估是否调整（届时新增 ADR）。

## ADR-010 本项目遵循 AI Agent 项目规范
- 2026-05-30 ・ 生效 ・ **决策** 本仓库自身按 AI Agent 项目规范运作：①所有决策落地到本决策记录；②每个主要目录添加 `AGENTS.md` 指导该目录作用、目标与规范。**理由** 以身作则、降低协作歧义、便于任何 Agent 冷启动。

## ADR-011 文档编号规范
- 2026-05-30 ・ 生效 ・ **决策** `docs/` 文件**按功能分类 + 优先级编号、中文文件名**。分类区段：`00–09` 治理与决策、`10–19` 研究与原则、`20–29` 规划、`30–39` 度量、`40–49` 分发与部署、`90–99` 归档。编号越小越基础/优先。**影响** 见 `docs/AGENTS.md` 索引。

## ADR-012 度量用途边界
- 2026-05-30 ・ 生效 ・ **决策** 度量数据用于发现瓶颈与验证规范有效性，**不作个人绩效考核**；只采流程元数据，不采代码/PII，可 `FPG_TELEMETRY_DISABLED=1` 退出。**理由** 避免古德哈特效应与生产力悖论的微观扭曲。

## ADR-013 移除 project-state（去 Python 的状态机制）
- **日期** 2026-05-30 ・ **状态** 生效
- **背景** 项目决定不再使用 Python；`.project-state.json` + `project_state.py`(MCP) 是唯一带外部依赖（`mcp`）的 Python 组件。其"跨会话进度"职责可由两条腿替代。
- **决策** **删除 `.project-state.json` 与 `project_state.py`**。跨会话进展由：① SKILL 从**项目产物**（`docs/PRD.md`、`docs/sprint-N.md`、`PROGRESS.md`、git log）派生；② 把进展信息（user、role、time、skill、milestone、phase、duration 等）作为**遥测事件经 `curl` 统一收集**（可度量/可观测/可改进）。
- **理由** 去 Python、去外部依赖；进度可观测性并入遥测；功能连续性用真实产物（更符合"结构化笔记"实践）。
- **影响** 所有 SKILL 的"前置检查/持久化"段重写；移除 MCP 配置与 `pip install mcp`；遥测事件 schema 增 `milestone`。

## ADR-014 工具脚本移植推迟 M2
- 2026-05-30 ・ 生效 ・ **决策** `init_project.py`、`generate_api_contract.py` 等"工具脚本"的**语言移植与功能改进推迟到 M2**；M1 仅移除 `project_state.py` 与 `mcp` 依赖。**理由** 聚焦 M1 的 SKILL 规范完备与分发；工具移植工作量大且独立。**影响** M1 期间这两个脚本仍为 Python（已在 milestone/计划标注）。

## ADR-015 解散 skills/shared，SKILL 自包含 + 项目级公共文件
- **日期** 2026-05-30 ・ **状态** 生效
- **背景** 原 `skills/shared/` 把多 SKILL 共享内容集中，既不利于"SKILL 自包含"，也使单个 SKILL 难独立分发。
- **决策** **解散 `skills/shared/`**：①**所有 SKILL 通用**的条目 → **目标项目 `AGENTS.md`**（随项目统一分发）；②**平台指南**（backend/web/ios/android、api-design）→ **项目级公共参考**（`project-template/`，安装时部署进目标项目）；③ SKILL **专属**内容 → 各自 `references/`。交付物因此包含**目标项目 AGENTS.md 等公共文件**，不只是 SKILL + 遥测。
- **理由** SKILL 自包含、可独立分发；通用约定单点维护、随项目分发。
- **影响** install 为每个 SKILL 建**软链**（默认项目级 `.claude/skills`+`.codex/skills`，可选用户级），**同名不覆盖**（跳过并告警）；并部署 `project-template/` 公共文件到目标项目。

## ADR-016 M1 重定义
- 2026-05-30 ・ 生效 ・ **决策** M1 重心 = **SKILL 规范完备**（内容覆盖 `docs/10-研究报告` 的实践）+ **支持 Claude Code 与 Codex** + **可分发可更新**；度量统一经遥测，**客户端仅 curl、尽量不依赖 TS**（收集器/报表后端仍 Bun/TS）。**理由** SKILL 是最重要资产，须先完备；M2 完成工具改进后，更新 SKILL 即可被统计。**影响** 部分修订 ADR-003（客户端不依赖 TS）。

## ADR-017 工具侧 hook 自动埋点（M2 提前，Codex 优先）
- **日期** 2026-05-30 ・ **状态** 生效
- **背景** 指令式 emit（SKILL 内一行可选 bash）遵从率仅 ~25–40%，且实测发现 Codex/Claude 用的**非交互 shell 不读 `~/.zshrc`**，导致 `FPG_HOME` 缺失、埋点静默跳过——真实使用产生 0 数据。
- **决策** 把"工具侧 hook 自动埋点"从 M2 提前实现，**Codex 优先**：用 **Codex Hooks**（`<repo>/.codex/hooks.json` 的 `SessionStart`/`Stop`）与 **Claude Code hooks**（`<repo>/.claude/settings.json`）在会话/回合事件**自动**上报 `session_start`/`turn_complete`，不依赖模型自觉。
  - 统一脚本 `telemetry/hooks/tool_hook.sh <codex|claude>`：两工具 payload 均为 stdin JSON，一个脚本服务两者；脚本**自带环境**（自 `source ~/.fpg-telemetry/env.sh`，规避非交互 shell 问题）；**永不阻断**工具（始终 exit 0、无 stdout）。
  - **不用 Codex `notify`**：`notify` 仅单程序（已被 Computer Use 占用）、仅 `agent-turn-complete`、payload 无 `cwd`；Codex Hooks 支持多源共存、项目级、stdin 带 `cwd`。
  - `install.sh --wire-hooks` 自动写入两工具 hook 配置，**已存在则跳过不覆盖**。
- **理由** "使用即度量"必须可靠；hook 由 harness 强制触发，不靠模型记得（对应研究报告 §6"运行时强制 / 环境优于提示"）。
- **影响** 工具 hook 提供可靠**粗粒度**（会话/回合/角色/工具/项目/时长），SKILL 内 emit 仍提供 best-effort **细粒度**（阶段/Story/契约/AC）。遥测事件类型新增 `session_start`/`turn_complete`。

## ADR-018 迭代治理层级与单一指标真源
- **日期** 2026-06-03 ・ **状态** 生效
- **背景** 近期使用 Codex 进行迭代开发时发现：Sprint 目录、Task 过程记录、milestone/feature/progress 状态、token 消耗和耗时统计如果散落在多份文件中，容易出现并行线程互相覆盖、指标冲突、进展不可度量的问题。
- **决策** 用户项目的迭代治理层级统一为 **Epic(E) → Sprint(S) → Task(T)**：
  - Epic 按创建顺序编号：`E001`、`E002`；Sprint 在 Epic 内编号：`S001`、`S002`；Task 在 Sprint 内编号：`T001`、`T002`。
  - 交互输入支持短编号别名：`e1-s1-t5` 必须归一化定位到 `E001/S001/T005`；目录仍使用三位编号以保证排序稳定。
  - **不再把 Milestone 作为用户迭代层级**；Milestone 只用于本仓库 M1/M2/M3 这类阶段性治理目标。用户项目中由 Epic 承担阶段目标、范围、验收、跨 Sprint 汇总和 backlog 归集。
  - 每一级只保留一份 `plan.md`，同时作为该级的**计划、直接子项清单、状态与指标真源**；不再额外维护同级 `INDEX.md`。
  - 父级 `plan.md` 只记录直接子级汇总，不复制孙级明细；Task 的 `plan.md` 记录自身目标、范围、验收、估计 token，`worklog.md` 记录过程，`smoke-report.md` 记录验证证据。
  - 启动和结束 Epic/Sprint/Task 时，必须在对应 `plan.md` 回填创建时间、开始时间、结束时间、主动耗时、等待耗时、估计 token、实际 token、偏差原因和证据链接。
- **理由** 用 `plan.md` 承担清单和指标总账可减少文件数量，避免计划与索引双写；按 E/S/T 编号可以稳定排序、便于并行任务边界和跨会话恢复；把 Milestone 与 Sprint 解耦可以保留阶段验收能力，同时避免概念过多。
- **影响** M2 新增 Epic `E001-iteration-governance`：新增 `project-template/references/iteration-governance.md`，并更新 `sprint-plan`、`sprint-develop`、`project-qa` 加载该参考；目标项目 `AGENTS.md` 补充迭代治理入口。`docs/01-里程碑与验收标准.md` 与 `docs/20-改进计划.md` 需引用该 Epic。

## ADR-019 度量计时主轴改为 Skill/切片，计划侧经 plan_sync 入遥测，身份 id/name 分离

- 2026-06-14 ・ 生效 ・ **背景** E002「统计细化」要求过程可控/可见/可量化/可改进。现状遥测看板只读 instrumentation（hook 实测），拿不到计划侧（E/S/T 描述/估算/状态/依赖/源链接）；身份只有不可读的 `actor_id`；且想沿传统 SDLC「阶段」计时，但 AI 辅助开发里设计/开发/测试常在同一会话交织，无干净启停时机。
- **决策**
  1. **取消传统阶段作为计时主轴**：不再用 `phase_enter/complete` 的 cycle-time 作主指标。计时单元改为有真实启停打点的 **Skill 调用**（`skill_start/complete`）与 **E·S·T 切片**（`.fpg/current-task` + hook 归因）。`phase` 字段保留为**软标签（活动类别，由 skill 派生）**，仅供人类归类。**审核/验收不是阶段**，是 `verification` 事件（点时刻 + outcome），喂返工率/AC 通过率。
  2. **计划侧入遥测**：新增 `plan_sync` 事件，由 `plan-sync.sh` 解析各级 `plan.md`（下级清单 + Mermaid 前置依赖 + 中文状态 + 估算 + 源链接）生成计划快照上报；`GET /stats` 把计划侧与实测侧 join 出 计划/实际/偏差。计划变更=重发覆盖（同 `project+root` 取最新）。**实际值绝不写回 plan.md**（plan-lint 已禁 `实际Token/主动耗时/等待耗时` 列）。
  3. **身份 id/name 分离**：`actor_id` 不可变（事件存它）；新增 `actors` 表存可改的 `display_name`，改名只更新该表、**不触碰历史事件**；`install.sh` 安装时询问显示名（默认 `Allen`）；历史 `actor_id` 默认显示名 `Allen`。
  4. **三独立维度**：Agent（claude/codex）、Skill（含 superpowers 等非内置）、Tool/MCP 各自成维，互不混；未调用 tool 记「无」。
- **理由** 只对有真实边界的单位计时，度量才可信（呼应 ADR-012「不自报」）；计划侧本已存在于 `plan.md`（ADR-018），缺的是流入遥测；id/name 分离满足「改名不影响已统计数据」。
- **影响** 新增 Epic `E002-metrics-refinement`（[plan](iteration/epics/E002-metrics-refinement/plan.md) / [design](iteration/epics/E002-metrics-refinement/design.md)）。`telemetry/schema.md` 升 `schema_version` 2 并加 `plan_sync`；`store` 加 `actors`/`user_prefs` 表；`dashboard` 重构为原生单页下钻+甘特。看板为零依赖原生单页（不引前端框架，对齐 ADR-002/016）。
