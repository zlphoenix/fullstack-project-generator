# 决策记录（ADR）

> 本项目自身按"AI Agent 项目规范"运作：**所有关键决策都落地到本文件**，可追溯、可评审、可版本化。
> 格式：编号 / 日期 / 状态 / 背景 / 决策 / 理由 / 影响。新决策追加在末尾；推翻旧决策时新增一条并把旧条状态改为「已废止（被 ADR-XXX 取代）」。

| 编号 | 决策摘要 | 状态 |
|---|---|---|
| ADR-001 | 同时支持 Claude Code 与 Codex | 生效 |
| ADR-002 | 后端覆盖 Java/Spring 与 TypeScript+Bun；含多项目/多环境/可观测等 | 生效 |
| ADR-003 | 度量采用集中式遥测（TS/Bun+shell），不复用 Python MCP、不用本地日志 | 生效 |
| ADR-004 | 交付文档中文为主 | 生效 |
| ADR-005 | 按 M1→M2→M3 推进，M1 先行再回看 | 生效 |
| ADR-006 | 零依赖 install.sh 分发；Skills-Manager 类工具仅调研 | 生效 |
| ADR-007 | 陈旧文件归档到 `docs/90-归档/` | 生效 |
| ADR-008 | 多 Agent 按可隔离上下文拆分，**不按角色/工作类型** | 生效 |
| ADR-009 | 保留迭代（Sprint）开发模式作为最小交付周期，迭代回顾用观测指标持续改进 | 生效 |
| ADR-010 | 本项目自身遵循 AI Agent 项目规范：决策落文档 + 每主要目录加 AGENTS.md | 生效 |
| ADR-011 | docs 文件按功能分类 + 优先级编号、中文文件名 | 生效 |
| ADR-012 | 度量数据用于改进与验证规范，不作个人绩效考核 | 生效 |

---

## ADR-001 工具范围：Claude Code + Codex 双支持
- **日期** 2026-05-30 ・ **状态** 生效
- **背景** 团队会同时使用多种 AI 编码工具；只面向单一工具会限制落地。
- **决策** 这套 Skill 必须在 **Claude Code 与 Codex** 上都可用；可引入 Skills-Manager 等开源分发工具支撑。
- **理由** 覆盖团队实际工具组合；`AGENTS.md`/`SKILL.md`/stdio MCP 均为跨工具标准。
- **影响** 以 `AGENTS.md` 为单一事实源；`install.sh` 同时配置两工具；细节差异需各自实测。

## ADR-002 技术栈范围
- **日期** 2026-05-30 ・ **状态** 生效
- **背景** 原仅 Java/Spring 后端，且缺多项目/多环境/可观测等长期维护能力。
- **决策** 后端覆盖 **Java/Spring 与 TypeScript+Bun**；并满足多项目管理、多环境隔离（dev/test/staging/prod）、本地开发友好（依赖一致性、独立运行、网络请求可观测）、运行时 HA/可观测/可扩展、可读性、CI+自动发布。
- **理由** 匹配团队真实技术栈与长期维护诉求。
- **影响** M3 落地（新 guide + 模板 + 环境规范）。

## ADR-003 度量：集中式遥测
- **日期** 2026-05-30 ・ **状态** 生效
- **背景** 需要在分发给所有成员后"使用即产生数据、统一收集"用于运营报表；本地日志无法集中。
- **决策** 独立的 `telemetry/` 集中式管线（emit.sh → Bun collector → Bun report），**不复用 Python MCP、不用本地日志**；语言仅 TS(Bun)+shell。
- **理由** 集中可汇聚、跨工具、零额外语言；与状态 MCP 解耦（状态记"是什么"，遥测记"发生了什么"）。
- **影响** 见 `30-度量指南.md`、`telemetry/`。

## ADR-004 文档语言
- 2026-05-30 ・ 生效 ・ **决策** 交付文档（研究/计划/README/度量等）**中文为主**，专有名词保留英文。**理由** 与团队沟通语言一致。

## ADR-005 执行节奏
- 2026-05-30 ・ 生效 ・ **决策** 分 M1（基座）/M2（强制与工件）/M3（栈扩展）三里程碑，**先做 M1 再回看**。**理由** 控制单次改动规模、尽早验证、降低审查负担。**影响** 见 `01-里程碑与验收标准.md`。

## ADR-006 分发工具
- 2026-05-30 ・ 生效 ・ **决策** 先用**零依赖 `install.sh`**；Skills-Manager 类外部工具仅在分发指南中作可选项调研，不引入依赖。**理由** 降低运维面，规模化后再评估。

## ADR-007 陈旧文件
- 2026-05-30 ・ 生效 ・ **决策** `SKILL.md.deprecated` 等陈旧文件移到 `docs/90-归档/` 保留追溯，不直接删除。**理由** 保留设计演进可追溯性。

## ADR-008 多 Agent 拆分维度（原则修正）
- **日期** 2026-05-30 ・ **状态** 生效
- **背景** 补充材料（[多 Agent 何时/如何用](https://claude.com/blog/building-multi-agent-systems-when-and-how-to-use-them)、[Anthropic harness-design](https://www.anthropic.com/engineering/harness-design-long-running-apps)、[OpenAI harness-engineering](https://openai.com/index/harness-engineering/)）指出：按角色/工作类型拆分会制造协调开销、交接丢上下文。
- **决策** 多 Agent **按可隔离的上下文拆分，绝不按角色/工作类型拆分**；先单 Agent，仅在"上下文污染/可并行/需专精"且有证据时才上多 Agent；生成者≠评估者；harness 随模型升级做减法。
- **理由** 降低协调开销与 token 成本（多 Agent 3–10x），提升验收可靠性。
- **影响** 研究报告 §5/§7 已更新；计划 WS0/WS3；待修订 `methodology.md §7.1`。注：遥测 `actor_role` 刻画**人类成员角色**，与此不冲突。

## ADR-009 保留迭代（Sprint）开发模式
- **日期** 2026-05-30 ・ **状态** 生效
- **背景** 基于对团队开发水平的评估，以及当前 AI 协作中存在的问题，暂不激进切换到纯交付驱动模式。
- **决策** **保留迭代开发模式，以 Sprint 作为最小交付周期**；每个迭代结束的回顾会**根据收集的迭代观测指标**（返工率、交付时长、AC 通过率、阶段周期时间等）评估并持续改进。
- **理由** 迭代节奏与团队现状匹配，且天然契合遥测度量与持续改进闭环。
- **影响** `methodology.md` 的 Sprint 流程保留；回顾会消费 `telemetry/` 报表；后续可随团队成熟度再评估是否调整（届时新增 ADR）。

## ADR-010 本项目遵循 AI Agent 项目规范
- 2026-05-30 ・ 生效 ・ **决策** 本仓库自身按 AI Agent 项目规范运作：①所有决策落地到本决策记录；②每个主要目录添加 `AGENTS.md` 指导该目录作用、目标与规范。**理由** 以身作则、降低协作歧义、便于任何 Agent 冷启动。

## ADR-011 文档编号规范
- 2026-05-30 ・ 生效 ・ **决策** `docs/` 文件**按功能分类 + 优先级编号、中文文件名**。分类区段：`00–09` 治理与决策、`10–19` 研究与原则、`20–29` 规划、`30–39` 度量、`40–49` 分发与部署、`90–99` 归档。编号越小越基础/优先。**影响** 见 `docs/AGENTS.md` 索引。

## ADR-012 度量用途边界
- 2026-05-30 ・ 生效 ・ **决策** 度量数据用于发现瓶颈与验证规范有效性，**不作个人绩效考核**；只采流程元数据，不采代码/PII，可 `FPG_TELEMETRY_DISABLED=1` 退出。**理由** 避免古德哈特效应与生产力悖论的微观扭曲。
